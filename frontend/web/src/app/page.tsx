import HomePage from '@/app/(MainApp)/HomePage';

/**
 * The main page component that renders the HomePage component.
 *
 * @returns {JSX.Element} The rendered HomePage component.
 */
const Page = () => {
    return <HomePage />;
};

export default Page;
